type Product = {
    name: string;
    version: string;
};
export type Data = {
    licence: string;
    api: string;
    key: string;
    product: Product;
};
export type API = {
    random: string;
    search: string;
    recommended: string;
};
export {};
